package stroom.test;

/**
 * A series of configuration values, must be kept in sync with the configuration resources
 */
public interface TestConstants {
    String ANIMALS_APP_CONFIG = "config.yml";
    int TEST_AUTH_PORT = 30080;
}
